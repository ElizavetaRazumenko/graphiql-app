import { styled } from '@mui/material/styles';

export const QueryEditorWrapper = styled('div')(() => ({
  padding: '0',
  borderRadius: '30px',
  background: 'rgba(255, 255, 255, 0.39)',
  boxShadow: '0px 4px 4px 0px rgba(0, 0, 0, 0.25)',

  flex: '1 0 50%',
}));
