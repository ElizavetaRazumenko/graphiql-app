import { styled } from '@mui/material/styles';
import { Button } from '../../../shared/Button';

export const ChangeEndpoint = styled(Button)(() => ({
  maxWidth: '120px',
  padding: '0 5px',
  fontSize: '13px !important',
}));
