import { ToggleButton, ToggleButtonProps } from '@mui/material';
import { styled } from '@mui/system';

export const ButtonLanguage = styled(ToggleButton)<ToggleButtonProps>(() => ({
  padding: '3px',
}));
